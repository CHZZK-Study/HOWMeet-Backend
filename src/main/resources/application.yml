spring:
  application:
    name: howmeet
  config:
    import:
      - classpath:/yaml/application-auth.yml
      - classpath:/yaml/application-db.yml
  profiles:
    group:
      develop: auth, db
    active: develop
springdoc:
  swagger-ui:
    url: /docs/SAMPLE - 0.0.1.json
    path: /docs/swagger